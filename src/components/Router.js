import { HashRouter as router , Route , Switch, Router } from "react-router-dom";

const AppRouter = () => {
    return (
        <Router>
         <Switch>
          <Route>

          </Route>
         </Switch>
        </Router>
        );
};

export default AppRouter;